name: Test

on:
  push:
   
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: sudo apt-get install libpcsclite-dev
      - run: ./ci/myna-build-linux-amd64.sh